import { BentoHome } from "@/components/bento/BentoHome";

/**
 * Home — the bento-desktop homepage. The previous editorial homepage (living
 * hero + work cards + about + contact) is archived at /work.
 */
export default function Home() {
  return <BentoHome />;
}
